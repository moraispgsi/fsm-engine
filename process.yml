apps:
  - script   : daemon.js
    instances: 0
    exec_mode: cluster
    env    :
      NODE_ENV: development
      DEBUG: core engine instance instance-log interpreter interpreter-actions
      REDIS_PASSWORD: @insticc@12345
    env_production:
      NODE_ENV: production
      DEBUG: instance-log
      REDIS_PASSWORD: @insticc@12345
